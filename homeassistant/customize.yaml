# Customizace entit pro změnu barvy ikon
sensor.nibe_degree_minutes_status:
  templates:
    icon_color: >
      {% set val = states('sensor.nibe_degree_minutes') | int(0) %}
      {% if val < 0 %}
        red
      {% else %}
        green
      {% endif %}
